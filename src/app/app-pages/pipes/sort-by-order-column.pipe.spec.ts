import { SortByOrderColumnPipe } from './sort-by-order-column.pipe';

describe('SortByOrderColumnPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByOrderColumnPipe();
    expect(pipe).toBeTruthy();
  });
});
